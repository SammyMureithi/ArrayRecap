const color = ["red", "Pink", "Yellow", "Dark"];
//we use unshift to removes  the  element at the first index of our array
//unshift returns the removed element of the  array 
color.shift();
console.log(color)